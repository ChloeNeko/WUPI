{
  "name": "wupi",
  "version": "0.10.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clear:webview2": "node scripts/clear-webview2-cache.cjs",
    "build": "npm run clear:webview2 && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "check": "node scripts/cargo-check.cjs",
    "release": "node scripts/release.cjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "adm-zip": "^0.5.16",
    "vite": "^6"
  }
}
